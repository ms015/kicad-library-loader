# SnapEDAの個別ZIPと一括ZIPの比較

検証日: 2026-09-17。対象: AP21510FM-7.zip / SnapEDA-Library.zip。KiCad 10.0.5。

## 結論

AP21510FM-7の端子割当とネット接続は両版で同じです。個別版はOUTの重複表示を隠しています。一括版は5番・6番を同じ位置に表示します。どちらかの端子割当が間違っている差ではありません。

現状の両者から選ぶなら、表示が重ならず、メーカー品番・出典リンク等の情報も豊富な個別ZIPを保持するのが適切です。ただし、どちらも旧来のピン重ね合わせであり、KiCad 10の現行KLC規約に合わせるなら5,6をネイティブピンスタックにするのが望ましいです。今回の作業では表示・ピンタイプ・接続を自動修正していません。

## 実データの差

| 項目 | 個別ZIP | 一括ZIP |
|---|---|---|
| シンボル形式バージョン | 20211014 | 20211014 |
| AP21510FM-7のピン数 | 7（露出パッド含む） | 7（露出パッド含む） |
| 5番OUT | 表示 | 表示 |
| 6番OUT | 非表示、5番と同じ座標 | 表示、5番と同じ座標 |
| 接続点・電気的タイプ・図形 | 6番の表示以外は同一 | 同左 |
| 属性 | MP、MF、SnapEDA_Link、価格等を含む | 属性が少ない。MANUFACTURER等は保持 |
| フットプリント | SON50P181X201X60-7N | 同じ形状、tedit時刻だけ異なる |
| STEP | AP21510FM-7.step | AP21510FM-70.step。バイト単位で同一 |

一括ZIPにはTC74HC595AF(EL,F)も含まれます。このため単なるファイル形式の世代違いではなく、個別／一括出力と付随属性の違いです。

## メーカーとの照合

[Diodes AP21510データシート](https://www.diodes.com/datasheet/download/AP21510.pdf) p.2の端子表では、1=GND、2=IN、3=EN、4=NC、5/6=OUT。OUTは外部で両方を接続する指定があります。露出パッドは放熱用GND面に接続し、通常のGND導通経路の代わりにしない指定です。両ZIPの割当はこれに整合します。

[KiCad KLC S4.3](https://klc.kicad.org/symbol/s4/s4.3/) はKiCad 10のネイティブピンスタックを使い、旧来の同じ位置にピンを重ねる方式を使わないよう定めています。ファイルをKiCad 10形式で再保存するだけでは、KLC準拠に自動変換されるわけではありません。

## KiCad実機能での検証

両シンボルをそれぞれ最小の試験回路に置き、重なったOUT接続点へ1本の配線とOUT_TESTラベルを接続して、KiCad 10 CLIからXMLネットリストを出力しました。

両版とも `/OUT_TEST` に `U1.5` と `U1.6` が含まれました。他の端子のネット一覧も一致しました。

ERC結果も両版で同一。試験回路はOUT以外を意図的に未配線とし、試験用Checkライブラリをグローバル登録していないため、未接続・電源未駆動・ライブラリ未登録等の指摘があります。ERCゼロを確認した完成回路という意味ではありません。

この検証はZIP間差分と端子割当に対するもので、実基板の動作やパッド寸法の製造適合性全般を保証するものではありません。3Dの位置合わせも未確認です。
